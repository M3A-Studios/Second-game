package game.blocks.special;
import game.Options;
import game.blocks.Blocks;
import game.entities.Player;

import java.util.List;

public class HiddenBlocks extends Blocks {
    public HiddenBlocks(int ID){
        super(ID);
    }
}
